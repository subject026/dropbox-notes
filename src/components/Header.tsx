import { Component } from "solid-js";

export const Header: Component = () => {
  return (
    <header class="p-4 bg-purple-600 text-purple-200 text-2xl font-bold">
      Markdown Thing
    </header>
  );
};

export default Header;
